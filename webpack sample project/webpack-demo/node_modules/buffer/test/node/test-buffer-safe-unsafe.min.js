"use strict";var Buffer=require("../../").Buffer;var assert=require("assert");var safe=Buffer.alloc(10);function isZeroFilled(a){for(var b=0;b<a.length;b++){if(a[b]!==0){return false}}return true}assert(isZeroFilled(safe));Buffer.allocUnsafe(10);assert(isZeroFilled(new Float64Array(10)));new Buffer(10);assert(isZeroFilled(new Float64Array(10)));Buffer.allocUnsafe(10);assert(isZeroFilled(Buffer.alloc(10)));